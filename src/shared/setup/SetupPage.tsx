import { StagePlaceholder } from '../../app/StagePlaceholder';

export default function SetupPage() {
  return <StagePlaceholder title="최초 설정 마법사" stage={5} source="G-class-reward InitialSetupWizard" />;
}
