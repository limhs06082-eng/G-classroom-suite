# 0.20.0 — 홈 2.0: 한눈에·바로 쓰기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/reference/home-2-and-class-board-proposal.md` §1. 홈에서 바로 적고 확인하고, 카드를 접고, 줄마다 역할이 있는 기본 배치, 도구 격자, 한 줄 머리띠.

**Architecture:** `SummaryCard`는 링크가 아니라 카드가 되고(머리에 [열기 ↗]와 `action` 자리), `HomeSlot`이 접기(늘 보이는 ^)를 맡는다. 배치에 `collapsed`가 늘고 크기는 카드별 기본값(`DEFAULT_SIZES`)을 갖는다 — 1도 저장한다. 새 카드 '오늘 알림장'(입력칸 내장), 출결 카드 [전원 출석 확인], 일정 카드 [+ 일정]과 D-day 열 개. 홈 아래 도구 격자(타이머·뽑기·투표·가리기·알림·오늘 보드). 머리띠는 lg부터 한 줄(아이콘만, 2xl에 글자).

## Tasks
1. `homeLayout.ts`·`types.ts`·`factories.ts`·`schema.ts`: `collapsed`, `sizeOf(layout,id,defaults)`, `resize`가 1도 저장, `setCollapsed/isCollapsed`. Tests: `tests/home/homeLayout.test.ts`, `tests/storage/homeLayoutSchema.test.ts`.
2. `SummaryCard.tsx` 재작성(카드+머리 [열기 ↗]+`action`, 여백 축소). `HomePage.tsx`: `HomeSlot`에 접기·아이콘·열기, 새 순서·`DEFAULT_SIZES`, '오늘 알림장' 카드(`NoticeInline`), 출결 [전원 출석 확인], 일정 [+ 일정] 모달·열 개 목록, 도구 격자, 머리 날짜. Tests: `tests/home/homeInline.test.tsx`.
3. `AppShell.tsx`: 내비 lg부터 한 줄(아이콘만), 날짜 표시.
4. 0.20.0 판 내기.
